// Define a set of color constants that will be used throughout the application
export const colors = {
  BACKGROUND: '#0E343D',  // A dark teal color, typically used for the background.
  HEADING: '#F9BE4A',     // A yellowish-gold color, usually for headings or important text.
  TEXT: '#FCE297',        // A light yellow color, used for regular text content.
  WHITE: '#FFFFFF',       // Standard white color, often used for background or text.
  BLACK: '#333333',       // A dark gray color, often used for text or backgrounds.
  CTA: '#F9BE4A',         // Call-to-action (CTA) button color, same as the heading color.
  TRANSLUCENT: 'rgba(255,255,255,0.1)', // Semi-transparent white color with 10% opacity, useful for overlays.
};

// Define a set of font families to ensure consistency across typography in the application
export const fontFamilies = {
  INTER_BOLD: 'Inter_18pt-Bold',        // 'Inter' font, bold style with 18pt size.
  INTER_REGULAR: 'Inter_18pt-Regular',  // 'Inter' font, regular style with 18pt size.
  LATO_BOLD: 'Lato-Bold',                // 'Lato' font, bold style.
  LATO_REGULAR: 'Lato-Regular',          // 'Lato' font, regular style.
  LATO_LIGHT: 'Lato-Light',              // 'Lato' font, light style.
};

// Define a set of common font sizes for text elements such as titles, body text, etc.
export const fontSizes = {
  XL: 20,  // Extra large font size, typically used for titles or headings.
  LG: 16,  // Large font size, used for subheadings or emphasized text.
  MD: 14,  // Medium font size, typically used for body text or paragraphs.
  SM: 12,  // Small font size, typically used for captions or footnotes.
};

// Define icon sizes to ensure consistent icon size across the application
export const iconSizes = {
  LG: 24,  // Large icon size, used for prominent icons or buttons.
  MD: 20,  // Medium icon size, used for regular icons.
};

// Define a set of spacing constants to create consistent padding and margin values
export const Spacing = {
  XS: 5,   // Extra small spacing, often used for very tight gaps or small paddings.
  SM: 10,  // Small spacing, used for minor margins or paddings between elements.
  MD: 15,  // Medium spacing, used for moderate gaps or padding between sections.
  LG: 20,  // Large spacing, used for larger gaps or padding.
  XL: 25,  // Extra large spacing, typically used for significant padding/margin or between sections.
};
